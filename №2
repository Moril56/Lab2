#include <stdio.h>

int main() {
    // Задані сталі величини
    const int a = 25, b = 2, c = -9;

    // Обчислення значення функції
    float y = (5 * a - b + c * c) / (2 + c) + 1;

    // Виведення результату на екран
    printf("Значення функції y = %.2f\n", y);

    return 0;
}
